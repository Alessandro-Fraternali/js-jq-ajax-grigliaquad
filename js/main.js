$(document).ready(function() {
  // ogni click sui div invia una richiesta AJAX che prende un numero random da 1 a 9 (primo end-point della API in slide).

  // Se il num ritornato è <= 5 il div diventa giallo

  // se è > di 5 il div diventa verde

  // Il numero ottenuto appare al centro del quadrato
});
